version: "3"

dotenv: [".env"]

tasks:
  dev:
    desc: Run Stack
    cmds:
      - go run main.go

  build:
    desc: Build the Stack binary
    cmds:
      - go build -o stack .

  build-and-run:
    desc: Build and run Stack
    cmds:
      - task: build
      - ./stack
